.title
  | Andy M. Young's Website
  P

- foo = Pinboard.new

.header
  | Pinboard Tags Example

.foo
  - bar = foo.getTags
  - bar.each do |x|
    = "#{x} "
  P

.header
  | Pinboard Posts Example
.foo
  - ruby = foo.getPosts("philo")
  - ruby.each do |y|
    a href="#{y[:url]}" #{y[:desc]}
    br
