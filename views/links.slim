.header
  | Link Tags

.foo
  - bar = @pinboard.getTags
  - bar.each do |x|
    - spacer = " - "
    a href="/links/#{x}" #{x}
    = spacer

P
.header
  ="#{@tag.upcase} Links"
.foo
  - ruby = @pinboard.getLinks("#{@tag}")
  - ruby.each do |y|
    a href="#{y[:url]}" #{y[:desc]}
    br
